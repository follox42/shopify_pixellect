<div class="hero">
  <div class="hero__container">
    <div class="hero__content">
      <h1 class="hero__title">Créez n'importe quelle expérience commerciale</h1>
      <p class="hero__description">
        Composez, personnalisez et étendez chaque partie de la pièce commerciale, pixel par pixel.
      </p>
      <a href="/collections/all" class="hero__button">Voir les produits</a>
    </div>
        
    <div class="hero__image-container">
      <div class="hero__image-wrapper">
        <!-- Image d'arrière-plan avec fondu multiple -->
        <img 
          src="{{ 'pixel-building-background.png' | asset_url }}" 
          alt="Bâtiment pixel art fond" 
          class="hero__image__background" 
          loading="eager"
        >
        
        <!-- Image principale avec fondu multiple -->
        <img 
          src="{{ 'pixel-building.png' | asset_url }}" 
          alt="Bâtiment pixel art" 
          class="hero__image" 
          loading="eager"
        >
        
        <!-- Ajout d'effets lumineux subtils -->
        <div class="hero__glow-effect"></div>
      </div>
    </div>
  </div>
</div>

<style>
.hero {
  padding: 80px 0;
  width: 100%;
  overflow: hidden;
  position: relative;
}

.hero__container {
  display: flex;
  justify-content: space-between;
  align-items: center;
  max-width: 1280px;
  margin: 0 auto;
  padding: 0 24px;
  gap: 40px;
}

.hero__content {
  flex: 1;
  max-width: 550px;
}

.hero__title {
  color: #fff;
  font-size: 54px;
  line-height: 1.1;
  font-weight: 700;
  margin-bottom: 20px;
  font-family: 'Montserrat', sans-serif;
}

.hero__description {
  color: rgba(255, 255, 255, 0.8);
  font-size: 18px;
  line-height: 1.6;
  margin-bottom: 30px;
  font-family: 'Montserrat', sans-serif;
}

.hero__button {
  display: inline-block;
  background-color: rgba(255, 255, 255, 0.1);
  color: #fff;
  padding: 14px 32px;
  border: none;
  border-radius: 4px;
  font-size: 16px;
  font-weight: 500;
  text-decoration: none;
  transition: background-color 0.3s ease;
  font-family: 'Montserrat', sans-serif;
}

.hero__button:hover {
  background-color: rgba(255, 255, 255, 0.2);
}

.hero__image-container {
  flex: 1;
  display: flex;
  justify-content: flex-end;
  max-width: 550px;
}

.hero__image-wrapper {
  width: 100%;
  height: 100%;
  position: relative;
  display: flex;
  justify-content: center;
}

/* Image principale avec masque complexe utilisant plusieurs dégradés */
.hero__image {
  max-width: 100%;
  height: auto;
  object-fit: contain;
  position: relative;
  z-index: 2;
  /* Combinaison de dégradés radiaux et linéaires pour un effet plus naturel */
  -webkit-mask-image: 
    radial-gradient(ellipse 85% 85% at center, black 60%, rgba(0,0,0,0.5) 80%, transparent 100%),
    radial-gradient(ellipse 100% 100% at 70% 30%, black 50%, transparent 100%);
  -webkit-mask-composite: source-in;
  mask-image: 
    radial-gradient(ellipse 85% 85% at center, black 60%, rgba(0,0,0,0.5) 80%, transparent 100%),
    radial-gradient(ellipse 100% 100% at 70% 30%, black 50%, transparent 100%);
  mask-composite: intersect;
}

/* Image d'arrière-plan avec dégradés multiples pour un fondu complexe */
.hero__image__background {
  max-width: 110%; /* Légèrement plus grand pour couvrir les bords */
  height: auto;
  object-fit: contain;
  position: absolute;
  z-index: 1;
  top: -5%;
  left: -5%;
  /* Combinaison de dégradés pour créer une dispersion naturelle */
  -webkit-mask-image: 
    radial-gradient(ellipse 70% 70% at center, black 20%, rgba(0,0,0,0.7) 50%, transparent 80%),
    radial-gradient(circle at 30% 70%, black 5%, transparent 50%),
    radial-gradient(circle at 70% 60%, black 5%, transparent 60%);
  -webkit-mask-composite: source-in;
  mask-image: 
    radial-gradient(ellipse 70% 70% at center, black 20%, rgba(0,0,0,0.7) 50%, transparent 80%),
    radial-gradient(circle at 30% 70%, black 5%, transparent 50%),
    radial-gradient(circle at 70% 60%, black 5%, transparent 60%);
  mask-composite: intersect;
  /* Effet de flou très léger pour les bords */
  filter: blur(1px);
}

/* Effet de lueur subtil pour améliorer l'ambiance */
.hero__glow-effect {
  position: absolute;
  width: 100%;
  height: 100%;
  z-index: 0;
  pointer-events: none;
  background: 
    radial-gradient(circle at 50% 50%, rgba(255, 170, 50, 0.05) 0%, transparent 50%),
    radial-gradient(circle at 80% 20%, rgba(255, 120, 30, 0.03) 0%, transparent 40%);
  filter: blur(20px);
}

@media screen and (max-width: 991px) {
  .hero__container {
    flex-direction: column;
    text-align: center;
  }
  
  .hero__content {
    max-width: 100%;
    margin-bottom: 40px;
  }
  
  .hero__image-container {
    justify-content: center;
    max-width: 100%;
  }
  
  .hero__title {
    font-size: 42px;
  }
}

@media screen and (max-width: 767px) {
  .hero {
    padding: 60px 0;
  }
  
  .hero__title {
    font-size: 36px;
  }
  
  .hero__description {
    font-size: 16px;
  }
}
</style>